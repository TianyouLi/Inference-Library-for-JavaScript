#define keep(t) extern void *__mod__##t##_KEEPME[]; void *__ref_##t = (void *) &__mod__##t##_KEEPME

keep(Convolution);
keep(Pooling);
keep(LRN);
keep(ReLU);
keep(Sigmoid);
keep(Softmax);
keep(TanH);
keep(AbsVal);
keep(Accuracy);
keep(AnnotatedData);
keep(ArgMax);
keep(BatchNorm);
keep(BatchReindex);
keep(Bias);
keep(BNLL);
keep(Concat);
keep(ContrastiveLoss);
keep(Crop);
keep(Data);
keep(Deconvolution);
keep(DetectionEvaluate);
keep(DetectionOutput);
keep(Dropout);
keep(DummyData);
keep(Eltwise);
keep(ELU);
keep(Embed);
keep(EuclideanLoss);
keep(Exp);
keep(Filter);
keep(Flatten);
keep(HDF5Data);
keep(HDF5Output);
keep(HingeLoss);
keep(Im2col);
keep(ImageData);
keep(InfogainLoss);
keep(InnerProduct);
keep(Input);
keep(Log);
keep(LSTM);
keep(LSTMUnit);
keep(MemoryData);
keep(MultiBoxLoss);
keep(MultinomialLogisticLoss);
keep(MVN);
keep(Normalize);
keep(Parameter);
keep(Permute);
keep(Power);
keep(PReLU);
keep(PriorBox);
keep(Reduction);
keep(Reshape);
keep(RNN);
keep(Scale);
keep(SigmoidCrossEntropyLoss);
keep(Silence);
keep(Slice);
keep(SmoothL1Loss);
keep(SoftmaxWithLoss);
keep(Split);
keep(SPP);
keep(Threshold);
keep(Tile);
keep(VideoData);
keep(WindowData);
